# SmartFileOrganizer
Smart File Organizer is a Java Swing desktop utility that monitors a folder in real-time using WatchService, automatically categorizes files, renames them with timestamps, and moves them into structured folders using a clean layered architecture.
